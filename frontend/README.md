# StudyFlow Frontend

Frontend для приложения StudyFlow — веб-приложения для управления учебными задачами и дедлайнами.

## Технологии

- **React 18** — библиотека для построения пользовательского интерфейса
- **TypeScript** — типизированный JavaScript
- **react-scripts** — сборка и dev-сервер (Create React App)
- **@dnd-kit** — библиотека для drag-and-drop функциональности
- **Axios** — HTTP-клиент для взаимодействия с API (заготовка для интеграции с backend)
- **Jest + React Testing Library** — unit-тестирование компонентов и API

## Функционал

### Реализовано
- 📋 **Kanban-доска** с тремя колонками: "К выполнению", "В процессе", "Выполнено"
- ➕ **Создание задач** с названием, описанием, дедлайном и приоритетом
- ✏️ **Редактирование задач** через модальное окно
- 🗑️ **Удаление задач** с подтверждением
- 🔄 **Drag-and-drop** перемещение задач между колонками
- 🔍 **Поиск и фильтрация** по статусу, приоритету и тексту
- 📊 **Статистика** — панель с прогрессом выполнения
- 🌓 **Тёмная тема** с сохранением настроек
- 🔔 **Toast-уведомления** при операциях
- 💾 **Хранение данных** в localStorage (работает без backend)

### Архитектура хранения
На текущем этапе данные хранятся в **localStorage** браузера. При подключении backend достаточно заменить реализацию в `api/tasks.ts` — интерфейс останется неизменным.

## Установка

```bash
npm install
```

## Запуск

### Режим разработки
```bash
npm start
```
Приложение откроется на http://localhost:3000

### Продакшен сборка
```bash
npm run build
```
Собранные файлы появятся в папке `build/`.

### Запуск тестов
```bash
npm test
```
Запускает unit-тесты в интерактивном режиме.

Для одноразового запуска (CI/CD):
```bash
npm test -- --watchAll=false
```

### Docker
Сборка и запуск через Docker:
```bash
docker-compose up --build
```
Приложение будет доступно на http://localhost:3000

## Структура проекта

```
frontend/
├── public/
│   └── index.html              # HTML шаблон
├── src/
│   ├── api/
│   │   └── tasks.ts            # Слой работы с данными (localStorage / API заготовка)
│   ├── components/
│   │   ├── Board.tsx           # Основной компонент с Kanban-доской
│   │   ├── Board.css           # Стили доски
│   │   ├── Statistics.tsx      # Панель статистики
│   │   ├── Statistics.css      # Стили статистики
│   │   ├── Toast.tsx           # Система уведомлений
│   │   └── Toast.css           # Стили уведомлений
│   ├── __tests__/
│   │   ├── tasks.test.ts       # Тесты API
│   │   └── Toast.test.tsx      # Тесты уведомлений
│   ├── types/
│   │   └── task.ts             # TypeScript типы
│   ├── index.tsx               # Точка входа React
│   ├── index.css               # Глобальные стили
│   └── setupTests.ts           # Конфигурация тестов
├── Dockerfile                  # Многоэтапная сборка
├── nginx.conf                  # Конфигурация nginx
├── .dockerignore               # Исключения для Docker
├── package.json
└── tsconfig.json
```

## Интеграция с backend (запланировано)

Backend находится в разработке. После его завершения:
1. Заменить реализацию в `api/tasks.ts` на HTTP-запросы через Axios
2. Backend будет доступен на `http://localhost:8000`

### API Endpoints (будущие)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/tasks` | Получить список задач |
| GET | `/tasks/{id}` | Получить задачу по ID |
| POST | `/tasks` | Создать новую задачу |
| PUT | `/tasks/{id}` | Обновить задачу |
| DELETE | `/tasks/{id}` | Удалить задачу |
